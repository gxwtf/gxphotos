# 人脸检索服务配置
# 服务监听地址（默认 localhost，如需外部访问可改为 0.0.0.0）
FACE_API_HOST=localhost

# 服务端口
FACE_API_PORT=8001

# Next.js 前端访问人脸服务的 URL
NEXT_PUBLIC_FACE_API_URL=http://localhost:8001

# GPU 配置（如需使用 GPU 加速，设置为 true）
# FACE_API_USE_GPU=false

# CPU 线程数（仅在非 GPU 模式下生效）
# FACE_API_THREADS=8